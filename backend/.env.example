# Copie para .env e ajuste. NUNCA commite o .env real.
PORT=3001
CORS_ORIGIN=http://localhost:5173

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=intranet_tecnotal

JWT_SECRET=troque-esta-por-uma-chave-longa-e-aleatoria
JWT_EXPIRES=8h

COOKIE_SECURE=false

# SMTP (deixe vazio em dev; preencha em producao)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Intranet TecnoTal"
FRONT_URL=http://localhost:5173
